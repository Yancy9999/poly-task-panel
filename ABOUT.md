# PolyTaskPanel

**版本**：2.1.1

多元任务面板：Web 界面管理 SpringBoot / Node 项目，一键启停、实时日志、进程树清理，并内置 Claude Code / Codex / pi 交互式终端。

---

## 2.1.1 更新

- **版本管理抽屉「提交历史」折叠**：Git / SVN 历史区改为抽屉式折叠条（默认折叠，钉在抽屉底部），点击平滑展开/收起，状态持久化到 localStorage。
  - 折叠态不发历史请求，打开抽屉/刷新更轻快；展开时有缓存直接渲染，无缓存才拉取。
  - 与「更改 / 无版本管理」分区同款交互：标题带 ▸/▾ 指示，操作过一次后记住选择。

## 2.1.0 更新

- **SVN 支持**：抽屉自动识别仓库类型——Git 仓库显示 Git 面板，SVN 工作副本显示 SVN 面板，同一入口无需切换。
  - SVN 面板：变更列表（修改/已新增/未版本控制分组）、勾选提交（勾选 = 只提交勾选项，不勾 = 提交全部变更）、一键 update 更新、提交历史（版本号/作者/时间/变更文件列表）。
  - 单文件 diff 双栏对比（工作区 diff 与历史任意版本 diff，复用 Git 的对比窗口）；未版本控制文件勾选即 `svn add`；变更文件右键撤回本地修改（`svn revert`，二次确认）。
  - 本机未安装 svn 命令行时给出安装引导；凭证沿用 svn 命令行缓存，非交互模式不弹认证。
  - 工作副本落后远端时历史仍取仓库全量（不受本地未 update 影响）。

## 2.0.0 更新

- **文件编辑器**：文件抽屉点击文件直接弹出可编辑窗口（多 tab、非模态、可拖动缩放），常态即编辑态（语法高亮 + 行号恒显）。
  - 多 tab：标题栏 tab 只显示文件名（悬浮显示完整路径），脏标记圆点；tab 拖动排序、右键菜单（关闭 / 关闭左侧 / 关闭右侧 / 关闭其他 / 关闭全部）；活动栏新增编辑器按钮（有 tab 才出现，点击唤回/收起窗口）。
  - 编辑与保存：Ctrl+S 保存、Ctrl+Z / Ctrl+Shift+Z 撤销重做（每 tab 独立快照栈，跨切换/保存保留）；保存时 mtime 冲突检测——文件被外部修改过则弹窗让用户选择重载或覆盖，不静默丢改动。
  - 新建文件 / 文件夹：文件树空白处与目录上右键菜单创建，严格校验文件名（拒绝路径分隔符与 Windows 非法字符），创建不打断当前展开状态。
  - 标题栏工具：保存（有未保存修改时出现）、重新加载磁盘内容（外部修改后手动刷新）、最大化 / 最小化（最小化保留 tab）。
- **文件抽屉**：头部新增全部展开 / 全部折叠图标按钮，按钮排布紧凑化。
- **Git 抽屉**：
  - 非仓库目录一键初始化（git init），初始化后进入空仓库状态可直接暂存提交。
  - 新增「远端」按钮：配置 / 修改 origin 地址后即可推送 / 拉取；未配置远端时隐藏拉取/推送/提交并推送按钮（点了必然失败），并给出引导提示。
  - 交互规范统一：成功不提示（状态刷新即反馈），只有失败才提示。
  - 布局调整：提交说明输入框与提交按钮上移至拉取/推送按钮下方。
- **深色主题统一**：文件编辑器窗口、全部弹窗（新建/确认/设置/命令查看/Git diff）、toast 通知、右键菜单、body 底色全部转深色，与终端区同一色板；`color-scheme: dark` 让原生控件跟随。
- **终端分栏**：激活态改为整栏顶部贯穿（header 整条提亮 + 顶部蓝条横跨全宽），与终端区连成一体，不再是小块 tab。
- **弹窗层级修复**：模态弹窗统一提到 z-index 400，文件编辑器窗口打开时设置等弹窗不再被盖住。

## 1.9.0 更新

- 文件抽屉过滤改为黑名单方式：默认仅隐藏 `.git` / `.svn`，`.gitignore`、`.env` 等其它文件（含点号开头）正常显示。
- 设置面板新增「文件」页：可自定义隐藏名单（每行一条，精确匹配名称），带「恢复默认」。
- 设置全部配置（字体 / 字号 / 命令 / 文件黑名单）改为持久化到服务端 `settings.json`，不再存浏览器 localStorage——重装应用 / 换端口后设置不丢；首次升级自动迁移旧 localStorage 配置。

## 1.8.1 更新

- 滚动条样式优化：项目列表、设置面板内容区滚动条改细（5px）、深色/浅色主题配色滑块，不再露出突兀的默认白色滚动条。

## 1.8.0 更新

- 新增文件内容查看：文件抽屉中点击文件弹出只读查看窗口，highlight.js 语法高亮（github-dark 主题）+ 行号；超 2MB 文件与二进制文件给出友好提示。
- 查看窗口桌面化交互：标题栏拖动、八方向缩放手柄、最大化/还原、双击标题栏还原默认大小；最大化态拖动标题栏自动还原。
- 查看窗口字体与终端一致：跟随设置面板的终端字体与字号，改设置即时生效。

## 功能

- **项目管理**：SpringBoot / Node / Folder 三类，新增 / 编辑 / 删除 / 拖拽排序，持久化到 `projects.json`。
- **一键启停**：启动 / 停止 / 重启；停止时递归 `taskkill /T` 杀进程树，不留孤儿进程占端口。
- **实时日志**：宿主项目日志 ANSI→HTML 渲染，可清空。
- **内置终端**：真 PTY（node-pty）+ xterm.js，在项目目录开交互式终端——Claude Code / Codex / pi / cmd，TUI 完整可用。
- **文件目录浏览**：左侧固定抽屉浏览项目目录树（可拖拽调宽）；支持在资源管理器中打开目录。
- **Git 管理**：左侧 Git 面板查看分支与变更、暂存 / 提交 / 拉取 / 推送、切换分支、浏览历史与 diff。
- **多栏分屏**：主区 1~4 栏并列，可同时查看多个会话 / 日志。
- **左侧活动栏**：项目 / 文件 / Git 三开关图标（底部齿轮为设置入口），点按展开对应面板、再点收起。
- **全部折叠 / 全部展开**：项目面板顶部一键折叠 / 展开所有项目卡片。

---

## 1.7.0 更新

- 布局重构：文件/Git 抽屉从右缘浮动叠层移到最左侧，与项目列表整合为左侧固定抽屉（活动栏 + 三面板），固定停靠挤压终端区，不再浮动遮挡、点外部不再自动收起。
- 活动栏移到窗口最左缘，按钮从上到下：项目、文件、Git；点击展开对应面板，再点同一按钮收起整个抽屉；三面板互斥，面板选择与抽屉宽度持久化。
- 设置齿轮按钮移到活动栏底部；项目面板底部不再有 footer。
- 移除侧栏整体折叠（36px 图标列）；项目卡片的全部折叠/展开按钮保留。

## 1.6.0 更新

- 终端区右缘新增活动栏（图标竖条）：文件目录浏览与 Git 抽屉的开关入口集中于此，图标随抽屉开态高亮；再点即收起，无参打开恢复上次状态。
- 分栏标题栏上的文件目录浏览按钮移除（入口统一到活动栏）；侧栏项目卡片上的 Git 按钮保留（打开指定项目的 Git 抽屉）。
- 新增 Git 管理：侧栏项目卡片新增 Git 按钮，打开右侧 Git 抽屉（与文件抽屉同构互斥，可拖拽调宽）。
- 状态查看：当前分支 + 变更文件列表（已暂存 / 未暂存分组，M/A/D/U/R 状态标着色）。
- 暂存 / 提交：勾选文件即暂存 / 取消暂存，输入说明一键提交。
- 拉取 / 推送：Pull / Push 按钮（依赖命令行已配好的远端凭证，失败原样返回 git 输出）。
- 分支切换：下拉列出本地分支，选择即切换。
- 历史与 diff：提交历史列表，点文件或提交弹出带增删行着色的 diff 预览。
- 非 git 仓库目录显示占位提示，其余功能不受影响。

## 1.5.2 更新

- 文件目录抽屉与分栏窗口解耦：右上角文件夹按钮改为纯粹恢复上次打开状态（项目目录 + 展开层级 + 选中条目），不再绑定当前激活栏项目。
- 抽屉完整状态（项目 / 展开层级 / 选中）持久化到 localStorage，重开应用后逐层还原。
- 唯一切换项目目录的入口改为侧栏项目卡片的文件夹按钮。
- 还原时自动清理已失效的展开路径（目录被删 / 改名不再反复报错）；删除抽屉正绑定的项目时状态同步清理回退空白。

## 1.5.1 更新

- 终端栏「/」常用命令发送逻辑优化：由「命令 + 空格 + 回车（100ms 间隔）」改为「命令 + Tab + 回车（50ms 间隔）」，避免 codex bracketed-paste 把整串误判为粘贴。

## 1.5.0 更新

- 侧栏顶部新增「全部折叠」「全部展开」项目卡片按钮（图标参考 IDA，上下两个尖括号向内 / 向外）。

---

## 桌面壳

可选 Tauri 打包为原生 Windows 应用（NSIS 安装包），自带 WebView2 窗口；打包时捆绑固定版本 Node 以匹配 node-pty 的 ABI。

MIT License
